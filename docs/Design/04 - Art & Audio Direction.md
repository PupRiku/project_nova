# 04 — Art & Audio Direction

> [!abstract] Status: **outline — to be authored together**
> Pins the visual/audio target and the pipeline into Godot. Owned with
> [[content-narrative]] for tone alignment.

## 1. Visual target
2D. Choose the register: `TODO` (pixel art / hand-drawn / flat vector).
Mood board refs: `TODO`

## 2. Resolution & scaling
Base resolution: `TODO` (project currently 640×360, integer-scaled).
Pixel-perfect? Camera rules? `TODO`

## 3. Palette & typography
Core palette `TODO`. UI font(s) `TODO`. Readability at target res.

## 4. Sprite specs
- Character sprite size / anchor / animation frame conventions `TODO`
- Enemy sprite conventions `TODO`
- Required animation states (idle, attack, hurt, ko, parry-react …) `TODO`

## 5. UI / HUD direction
Battle menu, HP/resource display, timing-window feedback visuals. `TODO`

## 6. Audio direction
Music tone/instrumentation `TODO`. SFX priorities — action-command cues are
critical (the player parries by sound as much as sight). `TODO`

## 7. Pipeline
Source tool → export settings → import into Godot → naming/foldering. `TODO`
Assets are Git LFS tracked (see `.gitattributes`).
